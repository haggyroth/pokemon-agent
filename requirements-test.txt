# Test/CI dependencies — the minimal set needed to run the hardware-free suite.
# (No openai/pygame/cffi: those pull in the LLM client, viewer, and native
# binding, none of which the pure unit tests import.)
pytest>=9.1.1
ruff>=0.15.21
requests>=2.34.2
python-dotenv>=1.2.2
